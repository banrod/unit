// Placeholder components registry used to satisfy renderer imports during testing.
import type { ComponentClasses } from '../system'

const components: ComponentClasses = {}

export default components
