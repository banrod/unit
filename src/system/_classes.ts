// Placeholder class registry to satisfy runtime imports during testing.
// Populate with concrete implementations when the upstream specification data is available.
import { Classes } from '../types'

const classes: Classes = {}

export default classes
